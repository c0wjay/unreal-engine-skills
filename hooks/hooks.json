{
  "description": "Unreal Engine Skills hooks. Claude Code sets CLAUDE_PLUGIN_ROOT; Codex auto-discovers this same file but exports its own PLUGIN_ROOT instead, so the command falls back to it. Without the fallback the path collapses to /hooks/unreal-context.sh and the hook exits 127 on Codex.",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|resume|clear",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT:-$PLUGIN_ROOT}\"/hooks/unreal-context.sh",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
